# Спецификация API вашего сервиса в формате OpenAPI 3.x. Пишете её на этапе 1,
# до кода: сначала проектируем контракт, потом реализуем.
#
# Что нужно (подробно — tasks/TASK-1.md, раздел «Контракт»):
#   - все операции из таблицы «Контракт API» в docs/ARCHITECTURE.md;
#   - у каждой операции summary и все коды ответов из таблицы, включая ошибки;
#   - схемы тел запросов и ответов в components/schemas.
#
# Проверка: make check-stage-1. Отступы — только пробелами, по 2.
openapi: 3.1.0
info:
  title: название вашего сервиса
  version: 0.1.0
paths: {}
