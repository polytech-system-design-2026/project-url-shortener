# Settings for teacher's contract tests. Used via `pytest -c contract_tests/pytest.ini`,
# so pytest settings in pyproject.toml do not affect them.
[pytest]
addopts = -ra -p no:cacheprovider
timeout = 180
markers =
    restarts_containers: stops or restarts docker compose services; exclude locally with -m "not restarts_containers"
